<?xml version="1.0"?>
<Container version="2">

  <!-- ═══════════════════════════════════════════════════
       CrowdSec Threat Map — Unraid CA Template
       Authors: kabelsalatundklartext, Railline
       https://github.com/Railline/crowdsec-threat-map-docker
       ═══════════════════════════════════════════════════ -->

  <Name>CrowdSec-Threat-Map</Name>
  <Repository>ghcr.io/railline/crowdsec-threat-map-docker:latest</Repository>
  <Registry>https://github.com/Railline/crowdsec-threat-map-docker/pkgs/container/crowdsec-threat-map-docker</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/Railline/crowdsec-threat-map-docker/issues</Support>
  <Project>https://github.com/Railline/crowdsec-threat-map-docker</Project>
  <Overview>
    CrowdSec threat map from your local CrowdSec database.&#xD;&#xA;
    &#xD;&#xA;
    Features:&#xD;&#xA;
    - English and French dashboard UI&#xD;&#xA;
    - Interactive D3 world map with animated attack flights&#xD;&#xA;
    - Live feed with search, filters and pagination&#xD;&#xA;
    - Optional live firewall drops panel, fed by a mounted JSONL file&#xD;&#xA;
    - Ban status and optional IP unban from the dashboard&#xD;&#xA;
    - Dynamic IP whitelist, themes, sound alerts and CSV export&#xD;&#xA;
    &#xD;&#xA;
    Requirement: CrowdSec must run as a Docker container on Unraid.&#xD;&#xA;
    &#xD;&#xA;
    Tip — find the CrowdSec data path:&#xD;&#xA;
    docker inspect crowdsec | grep -A5 "Mounts"
  </Overview>
  <Category>Tools: Network:Other</Category>
  <WebUI>http://[IP]:[PORT:8080]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/Railline/crowdsec-threat-map-docker/main/unraid/crowdsec-threat-map.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/Railline/crowdsec-threat-map-docker/main/unraid/icon.png</Icon>
  <ExtraParams>--group-add 999</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText>Gefällt dir das Projekt? Gib einen Stern auf GitHub!</DonateText>
  <DonateLink>https://github.com/Railline/crowdsec-threat-map-docker</DonateLink>
  <Requires>CrowdSec Docker Container</Requires>

  <!-- ═══════ PORT ═══════ -->
  <Config
    Name="Dashboard Port"
    Target="8080"
    Default=""
    Mode="tcp"
    Description="Port für das Web-Dashboard — frei wählbar, z.B. 8080, 5757, etc."
    Type="Port"
    Display="always"
    Required="true"
    Mask="false"></Config>

  <!-- ═══════ VOLUMES ═══════ -->
  <Config
    Name="CrowdSec Datenpfad"
    Target="/crowdsec/data"
    Default=""
    Mode="ro"
    Description="Pfad zum CrowdSec data-Verzeichnis (enthält crowdsec.db und optional GeoLite2-City.mmdb). Tipp: docker inspect crowdsec | grep -A5 Mounts"
    Type="Path"
    Display="always"
    Required="true"
    Mask="false"></Config>

  <Config
    Name="CrowdSec Postoverflows-Pfad"
    Target="/crowdsec/postoverflows"
    Default=""
    Mode="rw"
    Description="Pfad zum postoverflows-Verzeichnis. Wird für die dynamische IP-Whitelist benötigt (WHITELIST_ENABLED=true). Kann leer gelassen werden wenn Whitelist deaktiviert."
    Type="Path"
    Display="always"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="Docker Socket"
    Target="/var/run/docker.sock"
    Default="/var/run/docker.sock"
    Mode="ro"
    Description="Docker Socket — wird für IP-Unban und dynamische Whitelist (CrowdSec-Neustart) benötigt."
    Type="Path"
    Display="always"
    Required="false"
    Mask="false">/var/run/docker.sock</Config>

  <Config
    Name="Optional Drops Log Directory"
    Target="/crowdsec/drops"
    Default=""
    Mode="ro"
    Description="Optional directory containing drops.jsonl for the live firewall drops panel. Leave empty if you only want the translated dashboard."
    Type="Path"
    Display="advanced"
    Required="false"
    Mask="false"></Config>

  <!-- ═══════ PFLICHT-VARIABLEN ═══════ -->
  <Config
    Name="Server Breitengrad (LAT)"
    Target="SERVER_LAT"
    Default="0.0"
    Mode=""
    Description="Breitengrad eures Servers — erscheint als Zielpunkt auf der Karte. Tipp: Rechtsklick auf Google Maps → Was ist hier?"
    Type="Variable"
    Display="always"
    Required="true"
    Mask="false">0.0</Config>

  <Config
    Name="Server Längengrad (LON)"
    Target="SERVER_LON"
    Default="0.0"
    Mode=""
    Description="Längengrad eures Servers — erscheint als Zielpunkt auf der Karte. Tipp: Rechtsklick auf Google Maps → Was ist hier?"
    Type="Variable"
    Display="always"
    Required="true"
    Mask="false">0.0</Config>

  <Config
    Name="Server Name"
    Target="SERVER_NAME"
    Default="MeinServer"
    Mode=""
    Description="Anzeigename eures Servers im Dashboard (z.B. Hamburg, Berlin, Homelab)."
    Type="Variable"
    Display="always"
    Required="true"
    Mask="false">MeinServer</Config>

  <Config
    Name="Language"
    Target="LANGUAGE"
    Default="en"
    Mode=""
    Description="Dashboard language: en or fr."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">en</Config>

  <!-- ═══════ CROWDSEC-CONTAINER ═══════ -->
  <Config
    Name="CrowdSec Container-Name"
    Target="CROWDSEC_CONTAINER"
    Default="crowdsec"
    Mode=""
    Description="Name des CrowdSec Docker-Containers. Prüfen mit: docker ps | grep crowdsec"
    Type="Variable"
    Display="always"
    Required="true"
    Mask="false">crowdsec</Config>

  <!-- ═══════ WHITELIST ═══════ -->
  <Config
    Name="Dynamische Whitelist aktivieren"
    Target="WHITELIST_ENABLED"
    Default="true"
    Mode=""
    Description="Verhindert Selbst-Ban bei wechselnder IP (typisch bei deutschen Heimanschlüssen). Eigene IP wird automatisch alle 15 Minuten in CrowdSec gewhitelistet."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">true</Config>

  <Config
    Name="Whitelist Datei-Pfad"
    Target="WHITELIST_FILE"
    Default="/crowdsec/postoverflows/s01-whitelist/my-whitelist.yaml"
    Mode=""
    Description="Pfad zur Whitelist-YAML innerhalb des Containers. Nur ändern wenn ihr einen anderen Postoverflows-Pfad nutzt."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">/crowdsec/postoverflows/s01-whitelist/my-whitelist.yaml</Config>

  <Config
    Name="Whitelist Prüfintervall (Sekunden)"
    Target="WHITELIST_INTERVAL"
    Default="900"
    Mode=""
    Description="Wie oft die eigene IP geprüft und ggf. gewhitelistet wird. Standard: 900 Sekunden (15 Minuten)."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">900</Config>

  <!-- ═══════ ERWEITERTE EINSTELLUNGEN ═══════ -->
  <Config
    Name="Cache TTL (Sekunden)"
    Target="CACHE_TTL"
    Default="60"
    Mode=""
    Description="Wie lange die Metrik-Daten gecacht werden bevor sie neu aus der DB geladen werden. Standard: 60 Sekunden."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">60</Config>

  <Config
    Name="Tage zurück (DAYS_BACK)"
    Target="DAYS_BACK"
    Default="365"
    Mode=""
    Description="Wie viele Tage Angriffs-Historie angezeigt wird. Standard: 365 Tage."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">365</Config>

  <Config
    Name="Enable Live Drops"
    Target="DROPS_ENABLED"
    Default="false"
    Mode=""
    Description="Enable the optional /drops API. Requires a JSONL file mounted at DROPS_LOG_PATH."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">false</Config>

  <Config
    Name="Drops JSONL Path"
    Target="DROPS_LOG_PATH"
    Default="/crowdsec/drops/drops.jsonl"
    Mode=""
    Description="Path inside the container to the optional firewall drops JSONL file."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">/crowdsec/drops/drops.jsonl</Config>

  <Config
    Name="Drops Max Events"
    Target="DROPS_MAX_EVENTS"
    Default="200"
    Mode=""
    Description="Maximum number of live drop events returned by /drops."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">200</Config>

  <Config
    Name="Drops Max Age Seconds"
    Target="DROPS_MAX_AGE_SECONDS"
    Default="3600"
    Mode=""
    Description="Only keep drop events newer than this age. Use 0 to disable age filtering."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">3600</Config>

  <Config
    Name="CrowdSec Restart Wartezeit (Sekunden)"
    Target="CROWDSEC_RESTART_WAIT"
    Default="15"
    Mode=""
    Description="Wartezeit nach CrowdSec-Neustart (bei Whitelist-Update). Erhöhen wenn Timeout-Fehler im Log erscheinen. Standard: 15 Sekunden."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">15</Config>

  <Config
    Name="Unban API-Token"
    Target="UNBAN_API_TOKEN"
    Default=""
    Mode=""
    Description="Empfohlen: Zufälliges Token (z.B. openssl rand -hex 32). Schützt den Unban-Endpunkt vor unbefugten Aufrufen. Leer lassen nur im isolierten LAN."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="true"></Config>

  <Config
    Name="CrowdSec Restart Cooldown (Sekunden)"
    Target="CROWDSEC_RESTART_COOLDOWN"
    Default="300"
    Mode=""
    Description="Mindestabstand zwischen automatischen CrowdSec-Neustarts bei Whitelist-Updates. Standard: 300 Sekunden."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">300</Config>

</Container>
